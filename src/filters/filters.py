# # todo возможны фильтры
#
#
# class SimpleExampleFilter(BaseFilter):
#     def __init__(self): ...
#
#     async def __call__(self, msg: Message, state: FSMContext):
#         """return (await state.get_data()).get("...")"""
